<?php
$form = new RM_View_Form();
$form->getLangPanel()->allLangs();
$form
    ->add(new RM_View_Form_Field_Text('Name', 'name', ''));
if ($this->user->getRole()->isProgrammer()) :
    $form
        ->add(new RM_View_Form_Field_Text('Alias', 'alias', ''));
endif;
echo $form->render();